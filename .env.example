# .env
# This file contains environment variables for the application.
# In a real production environment, these should be set directly on the server.

# --- Security Keys ---
# You can generate new keys using: python -c 'import secrets; print(secrets.token_hex(16))'
SITE_KEY=site_key_12345
SECRET_KEY=secret_key_abcde

# --- CORS Origin ---
# The full origin (scheme, hostname, port) of the parent page hosting the captcha.
# For local development, this would be http://127.0.0.1:5000
ALLOWED_ORIGIN=http://127.0.0.1:5000
